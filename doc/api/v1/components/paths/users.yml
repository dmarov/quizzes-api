get:
  security:
    - jwtAuth: []
  summary: get list of users
  responses:
    '200':
      description: list of users
      content:
        application/json:
          schema:
            $ref: '../schemas/user.yml#/UsersHal'
  tags:
    - user

post:
  security:
    - jwtAuth: []
  summary: create user
  requestBody:
    description: username provider
    content:
      application/json:
        schema:
            $ref: '../schemas/user.yml#/UserName'
    required: true
  responses:
    '201':
      description: user created
      content:
        application/json:
          schema:
            $ref: '../schemas/user.yml#/User'
    '401':
      $ref: '../responses/not-authorized.yml'
  tags:
    - quizzes
